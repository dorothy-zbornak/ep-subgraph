{
  "name": "ep-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dorothy-zbornak/ep-subgraph",
    "create-local": "graph create --node http://localhost:8020/ dorothy-zbornak/ep-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dorothy-zbornak/ep-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dorothy-zbornak/ep-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
