{
    "name": "exchange-v4",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --studio 0x-exchange-proxy",
        "create-local": "graph create --node http://localhost:8020/ 0xProject/exchange-v4",
        "remove-local": "graph remove --node http://localhost:8020/ 0xProject/exchange-v4",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xProject/exchange-v4"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "^0.21.0",
        "@graphprotocol/graph-ts": "^0.20.1"
    }
}
